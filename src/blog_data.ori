blogs
→ (_) => Tree.map(_, (blog, fileName, _body) => {
	...blog
	type: "blog"
	file_name: Origami.basename(fileName)
})
→ (_) => Tree.sort(_, (blog) => blog.pub_time)
→ (_) => Tree.map(_, {
	extension: ".md->.html"
	value: (blog) => functions/parse-markdown.js(blog)
})
→ Tree.reverse
